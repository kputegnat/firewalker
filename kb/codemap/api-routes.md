# Code Map — API Routes
<!-- Budget: 400 lines. Route inventory: METHOD path → handler file, 1–2 lines each. -->
<!-- P2 scaffold (greenfield): empty by design — populated by workers in the same PR as route code (kb-same-PR rule). Planned sections mirror the target architecture (ADR-0014, PRD). -->

## /v1/sessions (planned — E01/E02)
(no routes exist)

## /v1/templates (planned — E06)
(no routes exist)

## /v1/submissions (planned — E05)
(no routes exist)

## Route-collision gate
Deferred until the API layer exists — binding noted in harness/config.json gate 8 (empty command = skipped with trace).
